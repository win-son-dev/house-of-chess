# Auth0 — fill these in from the Auth0 dashboard
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your-spa-client-id
AUTH0_AUDIENCE=https://api.houseofchess.local

# Postgres — only change for production deployments
POSTGRES_PASSWORD=postgres
